# Верхние границы обязательны: install.sh делает `pip install --upgrade -r`
# при каждом обновлении, поэтому мажорный релиз зависимости иначе попадёт в прод.
aiogram>=3.30,<4
aiohttp>=3.14,<4
qrcode>=8.2,<9
pillow>=12.3,<13

# Нужен для FSM-хранилища при заполненной настройке redis_fsm_url
# (aiogram.fsm.storage.redis). Без пакета бот молча откатывается на MemoryStorage.
redis>=5.0,<7
